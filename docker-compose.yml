services:
  kwp:
    image: tinyops/kwp:${KWP_VERSION:-latest}
    container_name: kwp
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
